---
layout: post
title: 角色扮演
categories:
  - LLM
tags: LLM
---
# 背景

如今，LLM在各种通用任务和各种评测上已经获取了相当显著的成绩，然而在具体场景落地的过程，由于开/闭源模型封闭源性质及其通用训练无法可以直观的量化在我们场景的作用。这时候就需要有一个练兵场，然而在现代的模型提供服务大多涉及多轮交互。模型本身的不稳定性/不同模型的多样性要求sanbox可以给出正确的响应/符合线上真实的情况，这时候就是角色扮演发力的地方。本文即谈论如何在沙盒评测环境中提供拟人（符合线上分布的）用户模拟器。虽然大模型能力够强，但是貌似引入的是一个循环依赖的问题，现有的开源大型语言模型主要在通用领域进行训练，缺乏针对角色扮演的特定优化。此外，虽然像 GPT-5.4 这样的最先进（SOTA）大型语言模型表现出先进的角色扮演能力，但它们的闭源性质带来了限制，包括高 API 成本、无法微调以及有限的上下文窗口大小。

![image.png](https://cdn.jsdelivr.net/gh/pointerhacker/image-bed@main/img/20260411220955445.png)
> 图一


## 引言

角色扮演（Role-Playing）是大语言模型（LLM）最具魅力的应用方向之一。它让模型不再只是一个冷冰冰的问答机器，而是能够化身为 Sherlock Holmes、Jack Sparrow、甚至孙悟空，以角色特有的口吻、知识和记忆与用户对话。

然而，现有的开源 LLM 大多在通用语料上训练，缺乏角色扮演的专门优化；而 GPT-4 等闭源模型虽然角色扮演能力出色，但存在 API 成本高、无法微调、上下文窗口有限等问题。

**RoleLLM** 这篇论文（Wang et al., 2024）正是针对上述痛点，提出了一套完整的角色扮演框架——从数据构建、评测基准到开源模型增强，形成了一条完整的闭环。本文将从以下几个核心维度对论文进行深度拆解：

1. **数据收集与角色档案构建**
    
2. **RoleGPT：角色提示与对话工程**
    
3. **Context-Instruct：基于上下文的指令生成**
    
4. **训练集****与****评测集****的构建**
    
5. **评测指标体系设计**
    

---

## 一、数据收集：从剧本到角色档案的全流程

### 1.1 剧本来源

论文的数据基础是大量的影视剧本（Scripts），来源包含三个渠道：

|   |   |   |
|---|---|---|
|数据来源|数量|语言|
|NLP Movie Scripts（GitHub 开源仓库）|661 个英文剧本|英文|
|SummScreen（学术数据集）|255 个英文剧本|英文|
|手动整理的中文剧本|24 个中文剧本|中文|

合计收集了 **916 个英文剧本** 和 **24 个中文剧本**，覆盖了电影、电视剧等多种类型。

### 1.2 角色筛选：从 940 个剧本中精选 100 个角色

角色筛选是一个多阶段的审核流程，核心目标是选出**说话风格高度独特**的角色：

**第一步：GPT-4 初筛。** 将剧本名称输入 GPT-4，让模型自动生成每个剧本的主角名称，并评估其说话风格的独特性（分为"不明显、有些明显、非常明显、极其明显"四个等级），同时给出评判理由。

**第二步：人工审核。** 对获得前两名风格独特性评分的角色进行人工验证，确认其剧本来源的准确性、说话风格描述的正确性，以及评分理由的合理性。

**第三步：GPT-4 复审。** 将通过初筛和人工审核的角色再次输入 GPT-4 进行独特性复评。

**第四步：对话轮次过滤。** 排除对话轮次少于 25 轮的角色——这些角色的剧本内容太少，不足以支撑后续数据生成。

最终精选出 **100 个角色**（95 个英文角色 + 5 个中文角色），涵盖了 30 个角色类别和 20 个剧本类别。这些角色各具特色，例如：

- **Jack Sparrow**（加勒比海盗）：海盗腔调，充满冒险感
    
- **Sherlock** **Holmes**：冷静理性，经典台词 "Elementary, my dear Watson."
    
- **孙悟空**（西游记）："俺老孙" 的口头禅
    
- **华妃**（甄嬛传）：傲慢霸道的宫廷说话风格
    

### 1.3 角色档案（Role Profile）的三要素构建

每个角色的档案由三个核心组件构成：

**（a）角色描述（Description）**

使用 GPT-4 生成角色的综合描述，涵盖性格特点、人生经历、性格变化、主要故事线和重要事件等。生成后由论文作者逐一验证。描述采用第二人称（"You are..."），便于后续作为系统指令使用。

以 Sherlock Holmes 为例，其描述为：

> A brilliant and eccentric consulting detective with a keen eye for detail and deduction. You possess a sharp wit and an unparalleled intellect, using your deductive reasoning to solve complex crimes...

**（b）口头禅（Catchphrases）**

同样使用 GPT-4 生成角色的经典台词或口头禅，例如 Sherlock Holmes 的 "Elementary, my dear Watson."，经人工验证后纳入档案。

**（c）结构化对话（Structured Dialogues）**

从剧本中通过正则表达式和规则方法自动解析对话内容。解析时不仅包含角色的台词，还保留旁白（narration）中的背景信息和事件描述。角色的动作也被整合到其对话内容中。

对话被组织为层级结构：单个对话轮次（turn）→ 对话回合（round）→ 幕/集（act/episode）。其中，每个对话"回合"以目标角色的对话内容结尾，确保大部分对话轮次都在角色的"可观察范围"内，即属于角色的情景记忆（episodic memory）。

---

## 二、RoleGPT：让 GPT 学会模仿角色说话

### 2.1 设计原则

在深入 RoleGPT 之前，先理解论文提出的两个核心设计原则：

**原则一：说话风格模仿（Speaking Style Imitation）**

- **词汇一致性（Lexical Consistency）**：模型回复应包含角色常用的口头禅或惯用表达。例如海盗角色应频繁使用 "matey"、"ahoy" 等航海术语。
    
- **对话保真度（Dialogic Fidelity）**：模型回复应在风格上与角色的范例对话保持一致，捕捉角色独特的句法和语气。
    

**原则二：角色特定知识与记忆注入（Role-Specific Knowledge Injection）**

论文将角色知识分为两类：

- **剧本内知识（Script-Based Knowledge）**：剧本中明确记录的细节，如角色背景、情景记忆、经历的具体事件等。
    
- **剧本外知识（Script-Agnostic Knowledge）**：角色可能具备但剧本中未明确提及的通识知识。例如扮演 Iron Man 时，模型应具备商业敏锐度和技术专长。
    

### 2.2 RoleGPT 是什么？

RoleGPT 是论文提出的一种**基于 GPT 的角色提示方法**，核心目标是利用提示工程让 GPT（尤其是 GPT-4）生成带有角色特征的高质量回复，用于后续数据集构建和作为评测的强基线。

### 2.3 RoleGPT 的三种提示策略

论文对比了三种提示策略，其中最后一种（少样本对话工程）是 RoleGPT 的核心创新：

**策略一：零样本****提示工程****（****Zero-Shot** **Prompt Engineering****, zsp）**

仅在系统指令中放入角色描述和口头禅，不提供任何示例对话：

```undefined
<|im_start|>system
You are Twilight Sparkle, ...<|im_end|>
<|im_start|>user
{user instruction}<|im_end|>
```

**策略二：少样本****提示工程****（Few-Shot** **Prompt Engineering****, fsp）**

在用户消息中拼接若干检索到的示例对话对，以传统的提示工程方式呈现：

```undefined
<|im_start|>system
You are Twilight Sparkle, ...<|im_end|>
<|im_start|>user
{Q1}\n{A1}
{Q2}\n{A2}
...
{user instruction}<|im_end|>
```

**策略三：少样本对话工程（Few-Shot Dialogue Engineering, fsd）⭐**

这是 RoleGPT 的核心创新。与传统的少样本提示不同，对话工程将示例对话对格式化为**多轮对话历史**，而非拼接在单条用户消息中：

```undefined
<|im_start|>system
You are Twilight Sparkle, ...<|im_end|>
<|im_start|>user
{Q1}<|im_end|>
<|im_start|>assistant
{A1}<|im_end|>
...
<|im_start|>user
{QN}<|im_end|>
<|im_start|>assistant
{AN}<|im_end|>
<|im_start|>user
{user instruction}<|im_end|>
```

### 2.4 为什么对话工程优于提示工程？

关键洞察在于：ChatGPT 和 GPT-4 这类对话模型已经将其上下文学习能力（in-context learning ability）交换为了对话历史建模能力。也就是说，传统的少样本提示（在一条消息中塞入示例）对这些模型的效果有限；而将示例以对话历史的形式呈现，更符合模型的训练范式。

GPT-4 评测结果验证了这一设计：

|   |   |   |
|---|---|---|
|提示策略|胜率（Win Rate）|平均排名|
|零样本提示工程（zsp）|9.3%|2.4|
|少样本提示工程（fsp）|29.8%|1.9|
|**少样本对话工程（fsd）**|**63.3%**|**1.5**|

对话工程的胜率是提示工程的两倍以上，显著优于其他策略。

### 2.5 RoleGPT 的检索增强

在对话工程的基础上，RoleGPT 还引入了**检索增强**机制：使用 BM25 算法从角色档案中检索与当前用户指令最相关的 top-5 对话对，作为少样本示例。这样，RoleGPT 的回复不仅能捕捉角色的说话风格，还能包含部分角色特定知识。

### 2.6 RoleGPT 与之前方法的区别

|   |   |   |
|---|---|---|
|对比维度|之前的方法|RoleGPT|
|角色粒度|粗粒度（职业、人格特质）|细粒度（具体角色，如 Sherlock Holmes）|
|提示方式|零样本或传统少样本|少样本对话工程|
|示例格式|拼接在用户消息中|格式化为多轮对话历史|
|知识注入|仅依赖模型自身知识|BM25 检索增强 + 系统指令|
|系统指令|简单角色描述|GPT-4 生成并人工验证的描述 + 口头禅|

---

## 三、Context-Instruct：从剧本中挖掘角色知识

### 3.1 Context-Instruct 是什么？

Context-Instruct 是论文提出的一种**基于长文本上下文的指令数据****生成方法**，核心目标是从角色的剧本档案中提取高密度的角色特定知识，生成高质量的问答（QA）训练数据。

RoleGPT 虽然能模仿说话风格，但由于档案中的检索信息稀疏且有噪声，知识发现效果有限。Context-Instruct 正是为了弥补这一不足。

### 3.2 Context-Instruct 与之前方法的区别

|   |   |   |
|---|---|---|
|对比维度|之前的方法（如检索增强）|Context-Instruct|
|知识来源|推理时检索，依赖检索质量|训练前从剧本中批量提取|
|知识密度|稀疏，受检索噪声影响|高密度，系统化提取|
|知识存储|存放在上下文窗口中，占用 token|编码到模型权重中，不占上下文|
|质量控制|无显式质量控制|置信度评分 + 去重 + 过滤|
|数据类型|仅通用指令的角色化回复|角色特定的 QA 对|

### 3.3 Context-Instruct 的三步流程

#### 第一步：角色档案分段（Role Profile Segmentation）

由于 GPT 的上下文窗口有限（论文使用时限制在 4096 token 内），需要将长篇角色档案切分为可管理的短片段。

**分段规则：**

1. 不在对话轮次中间截断
    
2. 每段至少 500 个英文单词（中文档案为 1000 字符），且至少包含 4 个对话轮次
    
3. 单个对话轮次不超过 500 个英文单词（中文为 500 字符），超出则丢弃
    
4. 每个角色的档案最多 100 个片段，超出则随机采样
    
5. 片段超过 2000 词时截断，确保输入 GPT 的 token 数不超过 4096
    
6. 不按照剧本的幕/集划分进行分段，因为这样会导致片段太少，影响后续数据生成的多样性
    

分段后，角色档案被分为两部分：

- **角色描述 + 口头禅**（作为特殊片段）→ 用于生成剧本外知识（script-agnostic）
    
- **剧本对话的多个片段** → 用于生成剧本内知识（script-based）
    

#### 第二步：指令与回复生成（Question-Confidence-Answer Triplet）

这是 Context-Instruct 最核心的创新。对于每个片段，使用 GPT 生成三元组 `[Q; C; A]`：

- **Q（Question）**：与给定片段相关的问题
    
- **C（Confidence）**：对问题质量的置信度评分及理由
    
- **A（Answer）**：对应的回答
    

**为什么需要****置信度****评分？** 论文在实验中发现，如果直接生成 QA 对（不加置信度），会出现两类质量问题：

- 对于剧本内 QA：问题常常不完整，因为默认读者已了解剧本上下文
    
- 对于剧本外 QA：问题常包含幻觉，因为缺乏上下文约束
    

受 Lin et al. (2022) 和 Xiong et al. (2023) 的启发，论文让模型同时生成置信度评分来评估问题的"完整性"或"事实性"。置信度分为高低两个等级，并要求模型给出评分理由。

**生成规模：**

- 剧本内三元组：每个片段在一次 GPT-3.5 调用中生成 3 个候选，最多 3 × 100 = 300 个，平均每角色约 150 个
    
- 剧本外三元组：角色描述驱动，单次生成 10 个候选，重复 20 次，共 200 个
    
- 若某角色的总候选数不足 400 个，则继续生成剧本外数据直至达标
    

**Prompt** **模板设计：** 生成 prompt 包含角色描述、口头禅、少样本示例和任务指令。

以中文剧本外知识生成为例，prompt 要求：

> 如果你有机会见到{script_name}中的{role_name}，你会向{role_name}提什么问题？设计10个问题...除了输出问题外，还需要输出你生成的每个问题的真实性(高还是低)和回复。

以英文剧本内知识生成为例，prompt 要求：

> Your task is to design {question_num} questions to ask the character {role_name}... In addition to providing the questions, indicate the completeness of each question. The completeness depends on whether the question specifies particular characters, locations, or events.

#### 第三步：数据过滤与后处理（Filtering & Post-processing）

**置信度****过滤：** 只保留最高置信度的候选，过滤掉约 30% 的数据。

**BM25 去重：** 使用 BM25 相似度对角色特定问题进行去重，移除约 66% 的剩余候选。

**训练/测试拆分策略：** 由于去重后不同样本的知识已不相关，传统的随机拆分不可行。论文采用以下策略——通过过滤保留的数据用于训练，被过滤掉的数据用于测试。具体而言，对于每个拥有超过 50 个被过滤问题的角色，选择基于 BM25 最不相似的 50 个问题作为测试集；否则保留所有被过滤问题。

---

## 四、训练集与评测集的构建：RoleBench

### 4.1 RoleBench 概览

RoleBench 是论文构建的**首个系统化、细粒度的角色级别角色扮演基准数据集**，包含 **168,093 个样本 / 23,463 条指令**。

### 4.2 构建的五个步骤

**步骤一：选择角色。** 如前文所述，从 940 个剧本中精选 100 个角色。

**步骤二：构建角色档案。** 包含 GPT-4 生成的描述和口头禅（经人工验证），以及从剧本中解析的结构化对话。

**步骤三：采样通用指令。** 从多个公开指令数据集中随机采样：

- 英文：从 Super-NaturalInstruct、UltraChat、Alpaca 中采样 **1,500 条**英文指令
    
- 中文：从 COIG、BELLE 中采样 **1,479 条**中文指令
    
- 所有指令均不超过 100 词，并基于 BM25 相似度去重
    

**步骤四：生成原始 RoleBench 数据。**

针对通用指令，使用 RoleGPT（GPT-4 API）为每个"角色-指令"对生成 6 条回复：

- 5 条作为 ground-truth 候选
    
- 1 条作为 RoleGPT 强基线用于性能对比
    

针对角色特定指令，使用 Context-Instruct（GPT-3.5 API）一次性生成指令和回复。

**步骤五：清洗 RoleBench。** 基于四个原则进行数据清洗：

1. **回复完整性**：排除回复不完整（缺少句末标点）的样本
    
2. **AI 身份隐藏**：排除回复中暴露 AI 身份的样本（如以 "As a language model" 开头）
    
3. **角色身份隐藏**：排除回复以角色名开头的样本（如 "Jigsaw: "）
    
4. **非拒绝**：排除模型拒绝回答的样本
    

### 4.3 RoleBench 详细统计

|   |   |
|---|---|
|指标|数值|
|角色类别数|30|
|剧本类别数|20|
|英文角色数|95|
|中文角色数|5|
|对话回合总数|140,726|
|总样本数 / 指令数|168,093 / 23,463|
|通用指令样本|147,609 / 2,979 条指令|
|角色特定指令样本|20,484 / 20,484 条指令|
|其中剧本外问题|13,220|
|其中剧本内问题|7,164|
|指令平均长度|25.71 词|
|回复平均长度|30.48 词|

### 4.4 数据质量验证

论文从通用和角色特定子集中各随机采样 100 个实例，邀请专家标注者从三个层面进行质量评估：

|   |   |
|---|---|
|质量审查维度|通过率|
|基础：回复是否回答了指令？|100%|
|中级：回复是否体现了角色的说话风格和性格特征？|84%|
|高级：回复是否正确且优雅地体现了角色的风格、性格和知识？|77%|

### 4.5 训练集与测试集的划分策略

论文在两个维度上进行训练-测试划分：

**维度一：指令泛化（Instruction Generalization）**

在同一批角色上，按 4:1 的比例划分指令（训练集 1200 条指令 : 测试集 300 条指令）。评估模型在已见角色上对未见指令的泛化能力。

**维度二：角色泛化（Role Generalization）**

仅限英文，按 8:1 的比例划分角色（80 个训练角色 : 10 个测试角色）。测试集包含每个未见角色 500 条通用指令样本（共 5000 条）以及所有角色特定指令。评估模型对完全未见角色的泛化能力。

---

## 五、评测指标体系

### 5.1 三类 Rouge-L 指标

论文使用 Rouge-L 衡量模型预测与 ground truth 之间的重叠度，但巧妙地设计了三种不同的 ground truth 来评估不同维度：

|   |   |   |
|---|---|---|
|指标缩写|Ground Truth 类型|评估维度|
|**RAW**|不带角色扮演的通用指令原始回复|回复准确性——模型是否正确回答了指令|
|**CUS**|RoleBench-general 中带角色扮演的定制回复|说话风格模仿——模型是否模仿了角色的说话风格|
|**SPE**|RoleBench-specific 中的角色特定回复|角色特定知识——模型是否掌握了角色相关的知识和记忆|

这三个指标的设计逻辑清晰：

- RAW 评估基础能力（能不能正确答题）
    
- CUS 评估风格模仿（答得像不像这个角色）
    
- SPE 评估知识深度（知不知道角色的故事和经历）
    

### 5.2 GPT 评估（GPT-4 as Judge）

受 AlpacaEval 的启发，论文使用 GPT-4 作为自动评估器，基于两个评判标准：

1. **说话风格特征**：哪个模型的角色说话风格更加明显，更符合角色描述
    
2. **角色知识丰富度**：哪个模型的输出包含更多与角色相关的知识和记忆
    

评估采用**胜率（Win Rate）形式：将每个模型与 RoleGPT 进行逐一对比，统计模型被 GPT-4 评为优于 RoleGPT 的比例。同时计算平均排名**指标。

为降低位置偏差（position bias），评测 prompt 明确要求 GPT-4 避免受回答顺序影响。

### 5.3 人工评估

论文邀请了 **3 名** **NLP** **方向的研究生**参与人工评测，采用与 GPT 评估相同的方法论——对比排名并计算胜率。

由于 Character.AI 不提供 API，其评估样本需手动在网站上操作并收集结果。因此，对于每种语言随机采样 500 条指令（包含通用和角色特定指令）进行评估。

### 5.4 评测成本控制

为控制 GPT API 成本：

- 指令泛化评估：每角色限制 20 条通用 + 20 条角色特定指令
    
- 角色泛化评估：每角色限制 50 条通用 + 50 条角色特定指令
    

---

## 六、RoCIT：角色条件化指令微调

### 6.1 训练方案

论文将 RoleGPT 生成的通用指令回复和 Context-Instruct 生成的角色特定 QA 数据混合，用于微调开源模型：

- **RoleLLaMA**：基于 LLaMA-7B，使用 LoRA 微调（rank=8, alpha=32）
    
- **RoleGLM**：基于 ChatGLM2-6B，同样使用 LoRA 微调
    

### 6.2 角色定制策略：系统指令 vs 检索增强

论文对比了两种角色定制策略：

|   |   |   |
|---|---|---|
|策略|方式|上下文开销|
|系统指令（sys）|在输入前添加角色名、描述、口头禅作为系统指令|低|
|检索增强（reaug）|从角色档案中检索相关对话对插入输入|高|

实验结果（在 SPE 指标上）：

|   |   |   |
|---|---|---|
|模型|检索增强（reaug）|系统指令（sys）|
|RoleLLaMA|36.7|**38.1**|
|RoleGLM|25.3|**34.1**|
|RoleGPT|23.6|23.4|

系统指令方案全面优于检索增强方案。论文推测，从角色档案中检索到的上下文示例含有噪声且稀疏，容易干扰相对较小的开源模型（如 RoleLLaMA 和 RoleGLM），导致性能下降。而更大的模型如 RoleGPT 对噪声更加鲁棒。

---

## 七、关键实验结果

### 7.1 指令泛化（英文）

|   |   |   |   |   |
|---|---|---|---|---|
|模型|CUS|RAW|SPE|平均|
|RoleGPT（GPT-4）|57.6|53.2|32.3|47.7|
|Character.AI|41.9|45.7|30.3|39.3|
|RoleLLaMA-7B|32.9|37.6|**38.1**|36.2|
|Alpaca-7B|24.2|35.3|27.0|28.8|
|Vicuna-13B|21.0|25.5|29.1|25.2|

关键发现：RoleLLaMA 虽然在风格模仿（CUS）和回复准确性（RAW）上落后于 RoleGPT，但在**角色特定知识（SPE）上反超 RoleGPT**，验证了 Context-Instruct 的有效性。

### 7.2 Context-Instruct 消融实验

|   |   |
|---|---|
|方法|SPE|
|RoleLLaMA（不含 Context-Instruct）|21.4|
|RoleLLaMA（检索增强，不含 Context-Instruct）|19.1|
|RoleLLaMA（含 Context-Instruct）|**38.1**|

Context-Instruct 将角色特定知识指标从 21.4 提升至 38.1，提升幅度接近 **80%**，效果显著。而检索增强甚至导致性能下降（从 21.4 降至 19.1），印证了噪声检索对小模型的负面影响。

---

## 八、总结与启示

RoleLLM 这篇论文的贡献可以总结为以下几个方面：

1. **首个系统化角色扮演基准**：RoleBench 包含 168K 样本，覆盖 100 个细粒度角色，是该领域首个开源的指令微调数据集和评测基准。
    
2. **对话工程优于****提示工程**：RoleGPT 证明了在对话模型上，将少样本示例格式化为对话历史比传统提示更有效。
    
3. **Context-Instruct 的知识提取优势**：通过置信度引导的三元组生成，显著提升了知识密度和数据质量，效果远超检索增强。
    
4. **系统指令优于检索增强**：对于小模型，将角色信息编码在系统指令中比检索增强更有效且上下文效率更高。
    
5. **开源模型可以逼近****闭源****模型**：RoleLLaMA 和 RoleGLM 在部分指标上已接近甚至超越 RoleGPT（使用 GPT-4），展示了开源模型在角色扮演领域的巨大潜力。
    

对于希望构建角色扮演系统的研究者和开发者来说，RoleLLM 提供了一套完整的方法论参考——从数据采集、角色建模、知识提取到模型训练和评测，每个环节都有清晰的设计原则和可复现的技术方案。